[project]
name = "world-models"
version = "0.1.0"
description = "World models from scratch in JAX: VAE, RSSM, actors in imagination, ViZDoom"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jax[cpu]",
    "flax",
    "optax",
    "distrax",
    "gymnax",
    "matplotlib",
    "numpy",
]

[project.optional-dependencies]
wandb = ["wandb"]
# GPU training runs under WSL2 (JAX has no native Windows CUDA support);
# see docs/gpu-setup.md. The marker keeps Windows installs CPU-only.
cuda = ["jax[cuda12]; sys_platform == 'linux'"]
# The engine. Ships the take_cover scenario and a free WAD; nothing to buy.
doom = ["vizdoom"]

[project.scripts]
ball-demo = "world_models.demo:main"
make-data = "world_models.data:main"
train-vae = "world_models.train_vae:main"
train-gru = "world_models.train_gru:main"
train-rssm = "world_models.train_rssm:main"
train-ac = "world_models.train_ac:main"
train-online = "world_models.train_online:main"
train-doom = "world_models.train_doom:main"

[dependency-groups]
dev = [
    "pytest",
    "jupyter",
    "ipykernel",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/world_models"]
